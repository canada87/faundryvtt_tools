<div class="generator-settings-content">

  <div class="form-group">
    <label>{{localize "CRAFTING.GeneratorCompendium"}}</label>
    <select name="compendiumLabel">
      <option value="">{{localize "CRAFTING.SelectCompendium"}}</option>
      {{#each availablePacks}}
        <option value="{{this.value}}" {{#if this.selected}}selected{{/if}}>{{this.label}}</option>
      {{/each}}
    </select>
    <p class="hint">{{localize "CRAFTING.GeneratorCompendiumHint"}}</p>
  </div>

  <div class="form-group">
    <label>{{localize "CRAFTING.GeneratorFolder"}}</label>
    <select name="componentsFolder">
      <option value="">{{localize "CRAFTING.SelectFolder"}}</option>
      {{#each availableCompendiumFolders}}
        <option value="{{this.name}}" {{#if this.selected}}selected{{/if}}>{{this.name}}</option>
      {{/each}}
    </select>
    <p class="hint">{{localize "CRAFTING.GeneratorFolderHint"}}</p>
  </div>

  <div class="levels-header">
    <h3 class="levels-title">{{localize "CRAFTING.GeneratorLevels"}}</h3>
    <button type="button" class="add-level-btn" data-action="addLevel">
      <i class="fas fa-plus"></i> {{localize "CRAFTING.AddLevel"}}
    </button>
  </div>
  <p class="hint">{{localize "CRAFTING.GeneratorLevelsHint"}}</p>

  <table class="levels-table">
    <thead>
      <tr>
        <th>{{localize "CRAFTING.GeneratorLevelNum"}}</th>
        <th>{{localize "CRAFTING.GeneratorLevelFolder"}}</th>
        <th>{{localize "CRAFTING.GeneratorLevelPoints"}}</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      {{#each levels}}
        <tr class="level-entry" data-level-index="{{this.index}}">
          <td class="level-num">{{this.num}}</td>
          <td>
            <input type="text" name="levels.{{this.index}}.folderName" value="{{this.folderName}}" />
          </td>
          <td>
            <input type="number" name="levels.{{this.index}}.points" value="{{this.points}}" min="1" />
          </td>
          <td>
            <button type="button" class="remove-level-btn" data-action="removeLevel">
              <i class="fas fa-trash"></i>
            </button>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>

  <button type="submit" class="submit-btn">
    <i class="fas fa-save"></i> {{localize "CRAFTING.Save"}}
  </button>

</div>
